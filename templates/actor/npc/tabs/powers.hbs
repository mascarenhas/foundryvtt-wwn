{{!-- NPC Powers tab: same body as PC; pool maxes stay editable via derivation. --}}
<section class="tab powers-tab {{tab.cssClass}}" data-group="{{tab.group}}" data-tab="{{tab.id}}">
  {{> wwnPowersTabBody}}
</section>
